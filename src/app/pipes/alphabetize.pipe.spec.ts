import { AlphabetizePipe } from './alphabetize.pipe';

describe('AlphabetizePipe', () => {
  it('create an instance', () => {
    const pipe = new AlphabetizePipe();
    expect(pipe).toBeTruthy();
  });
});
